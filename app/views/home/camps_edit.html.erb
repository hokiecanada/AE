<%= form_for @html, :url=> html_edit_path(@html) do |f| %>
	<%= hidden_field_tag "redirect", "camps" %>

	<div class="field">
		<%= f.text_area :camps, :cols => 78, :rows => 26 %>
	</div>
	<div class="actions">
		<%= f.submit "Save" %>
	</div>
<% end %>